import React from 'react';
import {Container} from "react-bootstrap";

const Basket = () => {
    return (
        <Container>
            BASKET
        </Container>
    );
};

export default Basket;